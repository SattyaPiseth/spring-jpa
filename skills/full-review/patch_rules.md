# Patch Rules

- Only generate a patch when explicitly requested.
- Keep diffs minimal, targeted, and reversible.
- Avoid sweeping refactors or formatting-only changes.
- Prefer configuration or small code fixes over large rewrites.
- If a fix is risky, document it instead of patching.
